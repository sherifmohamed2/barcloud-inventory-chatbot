# Barcloud Inventory Chatbot

Minimal inventory analytics chatbot that answers business questions and returns the exact SQL **“present query”** for each answer, using OpenAI/Azure OpenAI, a REST API, and a small web UI — implemented in **pure Python standard library with pydantic only** (no other external packages allowed).